class Service {
    static rejectResponse(error: Error, code = 500) {
        return { error, code };
    }

    static successResponse(payload: any, code = 200) {
        return { payload, code };
    }
}

export default Service;
